package com.example.upiexpensetracker.util

object AppConfig {
    // Set to false for production builds
    const val DEBUG_MODE = true
    
    // Debug features
    const val SHOW_TEST_BUTTONS = DEBUG_MODE
    const val ENABLE_LOGGING = DEBUG_MODE
}